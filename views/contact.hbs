<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Contact</title>
  </head>
  <body>
   <h2>Contact me</h2>
   {{#if isSent}}
   <div class="isSent">The message has been sent. The file {{name}} has been saved</div>
  {{/if}}
  
  {{#if isError}}
   <div class="isError">The message has not been sent. The file {{name}} has not been saved</div>
  {{/if}}

<form action="/contact/send-message" method="POST" enctype="multipart/form-data">
  <label>
    Your name: <input type="text" name="author">
  </label>
  <label>
    Your email: <input type="email" name="sender">
  </label>
  <label>
    Title: <input type="text" name="title">
  </label>
   <label>
    Message: <textarea name="message"></textarea>
  </label>
  <label>
    Choose file: <input accept="image/png, image/jpeg, image/jpg, image/gif" type="file" name="image">
  </label>
  <button type="submit">Send message</button>
</form>
  </body>
</html>